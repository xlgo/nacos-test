spring:
  profiles:
    active: prod
  application:
    name: platform-test
  cloud:
    nacos:
      config:
        server-addr: ${nacos.server:192.168.11.16:8849}
        username: ${nacos.username:nacos}
        password: ${nacos.password:nacos}
        namespace: ${nacos.namespace:public}
        file-extension: properties

  config:
    import:
      - nacos:${spring.application.name}.properties
